import { Stack } from '@mui/material'
import React from 'react'

const ChatItem = ({w="100%",chats=[], chatId,onlineUsers=[], newMessageAlert = [{chatId:"", count:0}]}, handelDeletecChat) => {
  return (
   <Stack></Stack>
  )
}

export default ChatItem